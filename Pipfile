# How to use this file:
# $ python -m pipenv install
# so a virtual environment will be created. Then, the tool can be executed with:
# $ python -m pipenv run python TAW.py
# or
# $ chmod +x TAW.py
# $ python -m pipenv run ./TAW.py

[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
pyyaml = "*"
tabulate = "*"
matplotlib = "*"

[dev-packages]

[requires]
python_version = "3.9"
